#include <stdio.h>

int return_bigger_value(int a, int b){
    if(a > b){
        return a;
    } else{
        return b;
    }
}

int return_smaller_value(int a, int b){
    if(a < b){
        return a;
    } else{
        return b;
    }
}